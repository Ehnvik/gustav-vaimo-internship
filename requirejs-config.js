// eslint-disable-next-line no-unused-vars
const config = {
  config: {
    mixins: {
      'mage/validation': {
        'Magento_Cms/js/mixin/custom-country-validation': true,
      },
    },
  },
};
